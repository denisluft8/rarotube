import { colors } from "./theme";

const lightTheme = {
  name: "light",
  colors: {
    primary: "#131313",
    secondary: colors["faux-smoky-grey"],
    switchSpan: "white",
    switchButton: colors["pink-sasquatch"],
    placeholder: "#555555",
    background: "#FFFFFF",
    boxShadow:
      "3px 3px 15px rgba(0, 0, 0, 0.2), inset 1px 1px 0px rgba(255, 255, 255, 0.3);",
    boxBackground: "rgba(216, 216, 216, 0.2);",
  },
};

export default lightTheme;
