import { colors } from "./theme";

const darkTheme = {
  name: "dark",
  colors: {
    primary: "#FFFFFF",
    secondary: colors["light-silver"],
    switchSpan: colors["pink-sasquatch"],
    switchButton: "white",
    placeholder: colors["light-silver"],
    background: "#131313",
    boxShadow:
      "3px 3px 10px rgba(0, 0, 0, 0.4), inset 1px 1px 0px rgba(255, 255, 255, 0.3);",
    boxBackground: "rgba(255, 255, 255, 0.1);",
  },
};

export default darkTheme;
